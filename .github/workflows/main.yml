name: Build Android APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install System Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            git \
            ffmpeg \
            libsdl2-dev \
            libsdl2-image-dev \
            libsdl2-mixer-dev \
            libsdl2-ttf-dev \
            libportmidi-dev \
            libswscale-dev \
            libavformat-dev \
            libavcodec-dev \
            zlib1g-dev \
            openjdk-17-jdk \
            unzip \
            zip \
            ccache \
            autoconf \
            libtool \
            pkg-config \
            libssl-dev \
            libffi-dev

      - name: Install Dependencies & Force Python 3.10
        run: |
          python3.10 -m pip install --upgrade pip setuptools
          python3.10 -m pip install "cython<3.0.0" "buildozer==1.5.0" "kivy==2.2.1"

      - name: Build APK with Buildozer Forced Python 3.10
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          python3.10 -m buildozer -v android debug

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Android-APK
          path: bin/*.apk
